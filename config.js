// Optional frontend API override.
// Leave empty when the FastAPI backend serves the frontend from the same origin.
// For a separate Netlify frontend, set this to your deployed backend URL.
window.MBWR_API_BASE_URL = window.MBWR_API_BASE_URL || "";
