# Copy to .env for local-only secrets if needed.
# Do not commit real API keys or tokens.

ALLOWED_ORIGINS=https://mood-based-watch-recommender.netlify.app,http://localhost:8000,http://127.0.0.1:8000
